# ─────────────────────────────────────────────────────────────────────────────
# platform-connectivity outputs.
#
# GENERATED FILE. Do not hand-edit. Rendered by Landing Zone Factory
# {{FACTORY-RAW:computed.factoryVersion}}.
#
# Workload spokes built later in this repository read these identifiers.
# ─────────────────────────────────────────────────────────────────────────────

#{{IF computed.topologyIsHubSpoke}}
output "connectivity" {
  description = "Hub-and-spoke pattern module outputs (VNet IDs, firewall IPs, DNS zones)."
  value       = module.hub_and_spoke_vnet
}
#{{ELSE}}
output "connectivity" {
  description = "Virtual WAN pattern module outputs (hub IDs, firewall IPs)."
  value       = module.virtual_wan
}
#{{ENDIF}}
