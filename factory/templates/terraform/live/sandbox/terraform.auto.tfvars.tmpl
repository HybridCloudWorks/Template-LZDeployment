# ─────────────────────────────────────────────────────────────────────────────
# Sandbox layer — a single time-boxed resource group for experimentation.
#
# GENERATED FILE. Do not hand-edit. Rendered from lz-config.json by
# Landing Zone Factory v{{FACTORY-RAW:computed.factoryVersion}}.
#
# This layer is emitted only when a sandbox environment is selected AND a
# sandbox subscription is supplied. Resources here are tagged temporary and are
# swept by the cleanup runbook in the platform-management layer.
# ─────────────────────────────────────────────────────────────────────────────

create_sandbox_rg = true

resource_group_name = "rg-sandbox-{{FACTORY-RAW:azure.primaryRegionCode}}-sbx-01"
location            = {{FACTORY:azure.primaryRegion}}

sandbox_tags = {
  environment  = "sandbox"
  lifecycle    = "temporary"
  created_date = "{{FACTORY-RAW:computed.generatedDate}}"
  owner        = {{FACTORY:operations.platformTeam.name}}
}
