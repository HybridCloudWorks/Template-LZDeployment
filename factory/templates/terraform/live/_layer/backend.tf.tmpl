# ─────────────────────────────────────────────────────────────────────────────
# Backend for the "{{FACTORY-RAW:layer}}" layer.
#
# GENERATED FILE. Do not hand-edit.
#
# The block is deliberately empty: every backend value lives in this layer's
# backend.hcl and is supplied at init time —
#
#     terraform init -backend-config=backend.hcl
#
# — so state location never hard-codes into committed Terraform, and the same
# root module re-inits cleanly if state is ever migrated.
#
# Each layer keeps an isolated state file. Sharing state across layers is the
# single most common way a landing zone becomes unrecoverable: a failure in one
# layer then locks or corrupts every other layer's state.
# ─────────────────────────────────────────────────────────────────────────────

terraform {
  backend "azurerm" {}
}
